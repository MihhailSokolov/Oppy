image: maven:latest

stages:
  - build
  - test
  - install

cache:
  paths:
    - .m2/repository/
    - target/

build:
  stage: build
  script:
    - mvn compile

test:
  stage: test
  script:
    - mvn test

install:
  stage: install
  script:
    - mvn clean install